# Baked into the iOS binary — point at your deployed API (must be reachable from the device).
# For simulator → host API, use your Mac's LAN IP, e.g. http://192.168.1.10:5000
VITE_API_URL=https://your-app.onrender.com
VITE_ROUTER=hash
