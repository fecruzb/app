/**
 * Article schema
 *
 * Tenant-scoped articles: title, Markdown body, optional cover, and an optional
 * `publishedAt` that surfaces the piece on the public marketing site.
 */
export { articles, type Article } from "./articles.schema";
